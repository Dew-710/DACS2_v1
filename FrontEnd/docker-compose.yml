version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      # Uncomment and set your backend API URL here if you are using environment variables
      # - NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
    restart: always
